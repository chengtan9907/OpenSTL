name: SimVP
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - numpy
  - hickle
  - pip
  - python
  - pytorch
  - xarray
  - pip:
    - scikit-image
    - timm
    - nni
    - tqdm
prefix: /opt/anaconda3/envs/simvp
