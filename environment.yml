name: SimVP
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - nni
  - numpy
  - hickle
  - pip
  - python
  - pytorch
  - timm
  - pip:
    - tqdm
    - scikit-image
prefix: /opt/anaconda3/envs/simvp